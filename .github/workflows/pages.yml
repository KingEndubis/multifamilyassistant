name: Deploy Becka Companion

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: syndicate-pro/package.json
      - name: Install deps (no lockfile)
        working-directory: syndicate-pro
        run: npm install
      - name: Build app
        id: build
        continue-on-error: true
        working-directory: syndicate-pro
        env:
          VITE_BASE: "/${{ github.event.repository.name }}/"
        run: npm run build
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Include Becka companion page
        if: steps.build.outcome == 'success'
        run: |
          mkdir -p syndicate-pro/dist
          cp syndicate-pro/ani.html syndicate-pro/dist/ani.html
      - name: Upload artifact (dist)
        if: steps.build.outcome == 'success'
        uses: actions/upload-pages-artifact@v3
        with:
          path: syndicate-pro/dist
      - name: Prepare fallback site
        if: steps.build.outcome != 'success'
        run: |
          mkdir -p fallback-site
          cp syndicate-pro/ani.html fallback-site/ani.html
      - name: Upload artifact (fallback)
        if: steps.build.outcome != 'success'
        uses: actions/upload-pages-artifact@v3
        with:
          path: fallback-site
      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v4